# Application
ENVIRONMENT=local
CORS_ALLOWED_ORIGINS=["http://localhost:3000"]

# Database
DATABASE_URL=

# Supabase Auth JWT validation
SUPABASE_URL=
SUPABASE_JWKS_URL=
SUPABASE_JWT_ISSUER=
SUPABASE_JWT_AUDIENCE=authenticated

# Optional, server-side only. Leave empty unless a future feature requires it.
SUPABASE_SERVICE_ROLE_KEY=
