name: API Deployment Placeholder

on:
  workflow_dispatch:

jobs:
  deployment-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain future workflow
        run: echo "Containerized backend deployment will be activated in Milestone 9."
