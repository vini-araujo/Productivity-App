name: Web Deployment Placeholder

on:
  workflow_dispatch:

jobs:
  deployment-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain future workflow
        run: echo "Automated static frontend deployment remains a future hardening task."
